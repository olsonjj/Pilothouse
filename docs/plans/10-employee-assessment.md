# 10: People Analyzer

**What to build:** Quarterly core-values scoring: per person, per quarter, a +/−/−− score against each active core value, with a rolled-up GWC summary column — the "right person, right seat" verdict view. Admin-only visibility and editing.

**Blocked by:** 07: Core values, 09: GWC ratings.

**Status:** done (review pending). Deltas documented in data-model.md:
- Grid columns: active core values, then inactive values that still hold scores
  for the selected quarter (historical scores stay visible; stable IDs).
- GWC summary rolls up ACTIVE assignments only (ended ones keep history but do
  not contribute); "right person / right seat" verdict derived server-side:
  Rate GWC first (any unrated) → Right person? Not yet (any false) →
  Exemplifies the values (all '+' on active values) → Not exemplifying
  (any '--') → Developing (otherwise).
- Scores are ADMIN-ONLY view AND edit (decided access rules — members get
  'forbidden' at the seam; the /people/analyzer route shows a plain
  "admins only" message to members).
- Migration generated by drizzle-kit (0008_add_people_analyzer).

- [x] Admin scores each person per quarter against each active core value (+ / − / −−)
- [x] One score per person/quarter/value (re-entering overwrites)
- [x] GWC summary column derived from current assignment ratings
- [x] Scores are visible and editable by admins only — members get denials at the seam
- [x] Scores survive core-value edits and deactivations (stable value IDs)
- [x] Quarter selector switches the scoring view
